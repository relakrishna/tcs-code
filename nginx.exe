pipeline {
    agent any
    stages {
        stage('nginx') {
            steps {
                sh 'echo hello'
            }
        }
        stage('learning') {
            steps {
                git url: https://github.com/relakrishna/tcs-code.git, 
                    branch: 'main'
            }
        }
    }
}